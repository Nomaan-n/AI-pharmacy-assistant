services:
  - type: web
    name: ai-pharmacy-assistant
    runtime: python
    branch: mcp-write-test-2026-08-19
    autoDeploy: true
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app.main:app --host 0.0.0.0 --port $PORT
    healthCheckPath: /health
